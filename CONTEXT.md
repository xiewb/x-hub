# x-hub Context

x-hub 是一个本地优先的个人效率工作台（Tauri 2 + Vue 3），围绕「启动、记录、打开、搜索」四大动作组织功能。本文档记录项目内容易混淆或需要精确区分的关键术语。

## 引导（Onboarding）

> ⚠️ **状态：规划中，尚未实施**（决策见 `docs/adr/0001-first-run-onboarding.md`；截至 v0.3.0 代码中不存在 OnboardingDialog / HelpView / onboarding_done / is_first_run，以下术语为实施前预登记，实现时按此命名）。

**快速设置弹窗（OnboardingDialog）**：
首次运行自动弹出的三步向导（外观 / 快捷键 / 数据与隐私），目标 <60 秒完成，可跳过。
_Avoid_：欢迎页、启动页（后者指每次启动都显示的 boot-splash 装饰页，纯展示无交互）

**帮助视图（HelpView）**：
侧栏常驻入口，按功能模块分节的图文说明页（每节「是什么 / 怎么用 / 设置在哪」三行），可随时重看首次引导。
_Avoid_：帮助中心、文档页

**首次运行判定（is_first_run）**：
仅当「配置缺 onboarding_done 标记」且「数据库核心表（resources/notes/todos/countdowns）全空」时判定为真·首次启动。老用户升级不误弹。
_Avoid_：只看配置字段默认值（会把老用户当新用户）

**onboarding_done**：
AppConfig 中的布尔标记，用户完成或跳过快速设置弹窗后置 true，之后不再自动弹出。
_Avoid_：first_run、seen_guide（语义不清）

## 桌面悬浮球（已实施）

> 术语为设计讨论中锁定的决策，实现（v0.5.0，ADR 0004）按此命名。

**桌面悬浮球（悬浮球）**：
桌面常驻的圆形入口小球，**仅主窗口隐藏时显示**（主窗口打开即收起）。可拖动到任意位置，**靠近屏幕边缘时**自动吸附但**不隐藏**（完整显示）。单击展开环形快捷菜单，双击显示主窗口，右键弹出与托盘一致的菜单，悬停球体 3D 光泽旋转。球体为 3D 玻璃质感（非扁平贴图）。
_Avoid_：悬浮窗（该词已被倒计时/便签等业务浮窗占用）、悬浮按钮

**环形快捷菜单（Radial Menu）**：
单击悬浮球后围绕球体展开的一圈圆形功能按钮，按钮指向现有视图；按钮集合在设置中可配置。靠边或空间不足时，悬浮球自动向左/右临时挪位以保证菜单完整显示，收起后回到吸附位置。
_Avoid_：面板（指面板式浮层，与环绕展开式不同）、九宫格（正交网格，不是圆周排布）

**吸附（Edge Snap）**：
悬浮球被拖到屏幕边缘附近时自动贴边停靠，但始终完整显示、不收起隐藏；离边缘较远时可自由停在任意位置。
_Avoid_：贴边隐藏（像任务栏自动收起只剩一条边那种，本功能明确不做）

**悬浮球常驻窗口**：
独立的透明无边框置顶小窗（复用 countdown_window.rs 的窗口模式），常驻存在，非惰性创建（Windows-only，`#[cfg(target_os = "windows")]` 隔离）。

## 界面组织

**视图（view）**：
侧栏主导航对应的主区页面，切换靠 index.vue 的 activeView 状态（无 Vue Router）。现有：工作台 / 速记 / 速达 / 设置 / 扩展中心 / 扩展（打开的单个扩展）/ 布局编辑器；「帮助」属引导方案（见上节，未实施），「待办」属待办升级方案（见下节，未实施）。

**分类（section）**：
设置页内部的锚点分区（常规 / AI助手 / 外观 / 工作台 / 快捷键 / 剪贴板 / 联网 / 扩展 / 数据 / 关于）。与「视图」不同层级。

## 速达

> 术语来自「速达功能优化」设计讨论（2026-09-24）。

**大类（major class）**：
速达资源的顶层归属，即 `resources.kind` 的三个值：应用 / 网页 / 文件。速达顶部的「全部」「常用」是筛选视图，不是大类。
_Avoid_：分类（已被设置页锚点占用）、标签、tab（那是筛选/浏览控件，不是归属）

**小类（subcategory）**：
大类下的二级归属，**单归属**——每条资源属于且仅属于一个小类，未指定时自动归入该大类的「默认」小类；**每个大类一套**小类库（允许同名不同义）。「文件」大类并入原 7 个内置 category 作为初始小类，「应用」「网页」初始为空。见 ADR 0012。
_Avoid_：标签（多选横切标记是另一套体系，见 ADR 0010）、分类（同上）

**默认小类（default subcategory）**：
每个大类自带的兜底小类，未指定小类的新条目自动归入；与「未归类」（存量条目没有小类、**保持空着不回填**）是两个状态。2026-09-25 实施口径（待用户确认）：可改名、可删除（删默认时排序最前的剩余小类自动晋升为默认）、暂无「隐藏」；文件大类的默认暂由「其他」承接。
_Avoid_：未分类（那是存量空小类）、把「默认」说成「其他」（「其他」只是文件大类的一个小类名）

**应用内打开（in-app open）**：
速达网页条目在应用自有 WebView 中加载的方式（载体两个：独立浏览窗、主窗口内嵌面板，均非 iframe，见 ADR 0011）；与「系统浏览器打开」相对，后者保留为常驻兜底出口。
_Avoid_：iframe 内嵌（被自家 CSP 与目标站双重拒绝，行不通）、内置浏览器（易读成完整浏览器产品能力）

**独立浏览窗（standalone browser window）**：
应用内打开的载体之一：带系统边框的独立 WebView 窗口（窗口内含自绘顶栏 webview 与外站页面 webview 两层），带轻量 tab 条（打开第二个页面才出现），从预创建常驻的窗口池取用（池 ×1，全部页面收进 tab）。
_Avoid_：浏览器窗口（易读成系统浏览器）、弹窗

**内嵌面板（embedded panel）**：
应用内打开的另一载体：主窗口内的 WebView 面板（非 iframe），单实例常驻、**单页无 tab**；布局同扩展 view 形态（主区整页视图：自绘工具栏 + 内容区子 webview）。默认打开方式可设置（设置 → 功能 → 速达），右键菜单可临时换用另一载体。
_Avoid_：iframe、侧边栏（侧栏是主导航）、分屏

**常用（Recent）**：
速达顶层的一个筛选维度：按最近启动时间排序的动态视图；它不是分类、不是收藏，不开放手动排序。
_Avoid_：收藏、星标（速达没有这两个概念）、最近文件（范围不对，常用含应用/网页）

## 待办（规划中，尚未实施）

> 术语为「待办功能升级」设计讨论（2026-09）中锁定的决策，代码实现前预登记。原需求里的「月待办 / 周待办」把两个不同概念混在一句话里，故此处强制拆成两组术语。

**待办视图（TodoView）**：
承载待办完整管理的独立视图（标签筛选、月/周日历、周期待办、描述、附件）；工作台「待办」卡片保持轻量，只负责快速录入与「今天干什么」的聚焦。
_Avoid_：待办中心、任务视图（「任务」在本平台另有所指）、待办页

**标签（tag）**：
挂在内容上的**可多选**横切标记，一条待办可有 0..N 个；有颜色。**待办标签与笔记标签是两套独立定义**（各自的定义表与关联表），互不同步——避免「改笔记标签把待办也改了」的意外。
_Avoid_：分类（该词已被设置页的锚点分区占用）、分组、笔记标签（那是另一套）

**日历模块（Calendar Module）**：
可从布局编辑器添加到工作台的独立模块，以月历形态展示各日期的待办分布（含周期待办的虚拟实例）。它是待办视图日历的**只读**呈现，**点击即跳转待办视图**做新建与改期；取代了原时钟模块的「整月日历」形态。
_Avoid_：待办卡片（那是快速录入 + 今天聚焦）、日历视图（那是待办视图内的模式，不是工作台模块）、整月日历（已被本模块取代的时钟模块旧形态，勿再引用）

**清单（list）**：
**单归属**的互斥容器，一条待办属于且仅属于一个清单（Things / 滴答清单式的 List）。本次**不做**，留待后续；与「标签」是两个不同概念，不可混用同一词。
_Avoid_：把「清单」当「标签」用，或反过来

**周期待办（recurring todo）**：
按规则反复发生的**一条**待办（每天喝水、每月交房租）；完成即滚动到下一轮，不逐次留下历史条目。它可以带子待办（一层）；勾选时若子项未完成会先确认，确认后一并勾选。
_Avoid_：重复任务、循环待办、周期任务

**置顶（pinned）**：
把待办固定在列表**最顶部「置顶」区**的标记，**与日期无关**；置顶条目脱离日期分组，因此周期待办滚动换组后仍留在置顶区。它只影响列表分组，不影响日历。
_Avoid_：收藏、星标、重要（那是优先级 priority，0-2 三档，与置顶不是一回事）

**虚拟实例（virtual occurrence）**：
按周期规则算出的**未来**待办实例，只用于日历渲染、**不落库**；与之相对，库里真实存在的那一行叫**当前实例**。虚拟实例不可拖拽改期（没有可改的实体）。
_Avoid_：未来待办、预生成实例（后者会被误读为已落库）

**时间范围视图（horizon）**：
按时间范围看「有哪些待办」的筛选视角——今天 / 本周 / 本月 / 全部；与「周期待办」正交（周期待办也会出现在范围视图里）。
_Avoid_：月待办、周待办（易被误读成周期待办）、日历（那是展示形态，不是筛选视角）

## 主题与外观

**应用壁纸（App Wallpaper）**：
用户为整个主窗口设置的自定义背景图片，所有视图共用一张；独立浮窗（便签/倒计时/提示词等小窗）不跟随。设置壁纸后背景以壁纸为准（取代主题渐变预设的背景），主题光晕氛围仍叠加其上。
_Avoid_：工作台壁纸（「工作台」在词汇表中特指 Dashboard 视图，易误读为仅该视图生效）、桌面壁纸（与操作系统桌面无关）

**卡片玻璃透明度（Glass Opacity）**：
全局玻璃卡片表面的透底程度，控制背景（应用壁纸或主题渐变）透过卡片的可见度；不设壁纸时也可独立调节玻璃质感。默认不透明（现状观感）。
_Avoid_：界面透明度、壁纸透明度（范围不清，易误以为连弹窗/浮窗一起变透明）；AI 面板透明度是另一回事（仅作用于对话面板，两者互不影响）

**壁纸蒙版（Wallpaper Veil）**：
罩在应用壁纸上的主题底色薄层（亮色模式近似提亮蒙纱、暗色模式近似压暗），用于在「壁纸鲜亮程度」与「文字/图标对比度」之间取平衡；与卡片玻璃透明度各自独立。
_Avoid_：壁纸透明度（与卡片玻璃透明度混淆）、压暗（只在暗色模式成立，亮色模式是提亮）

**沉浸模式（Immersive Mode）**：
壁纸的完整展示形态：卡片改用局部取景毛玻璃（backdrop-filter，性能红线的受控例外，见 ADR 0003），壁纸在卡片间隙保持清晰，卡内文字靠局部模糊保证可读；默认关闭。开启时整屏静态模糊（见背景模糊）自动让位，两者不叠加。
_Avoid_：玻璃透明度（那是卡片透底程度，不是取景方式）、全屏模糊（沉浸模式的要点恰是间隙不模糊）

**文字光晕（Text Halo）**：
壁纸态下给文字/图标垫的描边级光晕，方向跟随主题模式（亮色模式白晕托深墨、暗色模式黑晕托浅墨），任何壁纸上保底可读；分两级——铬件（侧栏/标题栏）常开描边级，卡片仅在真实透底时（低玻璃透明度或沉浸模式）开弱一档柔光；不遮壁纸、不做设置项。
_Avoid_：投影（drop-shadow 仅用于图标，text-shadow 用于文字）、蒙版（蒙版作用于壁纸整体，光晕作用于文字）

**铬件（Chrome）**：
不含内容的界面骨架——标题栏与侧栏。任何壁纸形态下都保持全透明，与背景（渐变或壁纸）构成同一个连续平面；表面材质只属于卡片。
_Avoid_：导航区、框架（语义不清）；给铬件垫玻璃/蒙纱（观感与背景割裂，已明确弃用）

**背景模糊（Background Blur）**：
应用壁纸的整体模糊开关。模糊作用于壁纸层全体——包括卡片之间空隙透出的部分——而非「仅卡片背后区域取景模糊」的局部毛玻璃；后者在 x-hub 中被性能约束禁用（见 ADR 0002）。
_Avoid_：毛玻璃模式（未说明是整屏柔化还是卡片局部取景）

## 扩展与市场

> 本节术语来自「扩展市场开放给第三方开发者」的设计讨论（2026-09）与随后的实现。前半部分是宿主侧行为（已实现），后半部分是平台治理流程（服务端已实现，见 `docs/open-market-client-plan.md`）。

**扩展根（Extensions Root）**：
已装扩展的唯一来源位置：`data_root()/extensions/<扩展 id>/`（便携版跟随 exe 目录）。扫描、热更新戳、入口加载全部以它为根；`.` 开头的目录（`.backup`、`.tmp-update`）被扫描与戳计算跳过。
_Avoid_：扩展目录（会与「某个扩展自己的目录」混淆）、插件目录

**本地安装（Local Install）**：
把打包产物（`.xhpack` / `.zip`）解包、校验 manifest 后**复制**进扩展根的安装方式；安装完成后源包与本机源码目录再无关联。
_Avoid_：本地调试、本机源码目录直挂（两者不是一回事——本地安装是一次性拷贝，改源码不生效）

**本机源码目录直挂（旧称「开发者模式」）**：
在**真实宿主内**直接加载本机**源码目录**（不复制进扩展根）进行调试的方式：在扩展中心「我的扩展」标签页加上目录即生效（**没有开关**），目录即真源，文件变更自动重载，并享有真实数据、真实权限与真实 service 后端。
_Avoid_：本地安装（拷贝式安装）、预览（指浏览器 + mock 桥的离线预览环境，不在宿主内、拿不到真实数据）、热更新（指已装扩展目录变动被扩展中心感知，与开发调试无关）

**开发扩展（Development Extension）**：
以源码目录直挂方式加载、尚未构成「已装扩展」的扩展。它不出现在已安装清单中，也不参与市场更新与卸载流程。
_Avoid_：本地扩展、临时扩展

**发布（Publish）**：
作者在客户端把某个版本的扩展打包上传、经平台关卡与人工审核并签名后进入市场清单的**动作**。清单永远只有平台一个签名者。
_Avoid_：上架（那是动作完成后的状态）、上传（上传只是这个动作的第一步）

**提交（Submission）**：
一次「送审」的流水记录：某个扩展的某个版本 + 它的关卡结论与审核状态。同一扩展每次发版都是一条新提交，因此**提交是流水，不是扩展本身**。
_Avoid_：把"提交"当成扩展的当前状态；版本（那是扩展的属性）

**关卡（Gate）**：
提交后由平台**自动**跑的一组检查（包结构、manifest、权限申报、静态扫描、版本约束）。它只做机器能判的事：通过不等于上架，未通过时逐项结论回传给作者。
_Avoid_：审核（特指人工终审那一步）、预检（作者在本机跑的那一份）

**预检（Precheck）**：
发布前在客户端本地跑的一份检查，口径与关卡一致，目的是让作者在点「发布」之前就发现问题。**它不构成放行依据**——以服务端关卡与人工审核为准。
_Avoid_：关卡（服务端那份才是权威）

**上架（Listed）**：
一次提交通过人工审核、被写入市场清单后的**状态**。清单里的条目是「某个扩展的某个版本」，所以同一扩展在不同时间可能对应不同的在架版本。
_Avoid_：发布（动作 vs 状态）

**撤销（Revoke）**：
平台撤回某个**已上架版本**的动作。客户端对"已装且被撤销的版本"只做两件事：在扩展中心标红警示、停止推送该版本；**绝不静默卸载或禁用**（本地优先原则）。
_Avoid_：卸载（用户侧动作）、封禁（针对开发者账号，与单版本撤销不同）

## 配置

**AppConfig / app.json**：
全字段带 serde default 的 JSON 配置（数据根下 app.json），新增字段天然兼容老配置。前端经 tauriApi.saveConfig 整体保存。

# v0.7.0 发布说明

**新增**

- 「速达」里的网页可以在应用内直接打开了：默认在主窗口的内嵌面板中显示，也可以在设置里改为「独立浏览器窗口」（支持多标签，同一个网址重复打开会自动回到已开的那个）；右键任何网页条目可临时换另一种方式，遇到打不开的网站还能一键转回系统浏览器。
- 「速达」的应用、网页、文件各有一套「小类」：在 设置 → 功能 → 速达 里可以新增、改名、拖拽排序，点星标设为默认——新添加的资源会自动归入默认小类；原来文件的 7 个分类原样保留，旧资源不受影响。
- 工作台新增「自定义速达」卡片（共 4 个槽位）：手动挑选几个最常用的，或直接绑定整个大类、某个小类，点一下就能启动。

**优化**

- 隐藏到后台的窗口（对话窗、剪贴板、悬浮球、应用内浏览器等）会自动把占用的内存交还给系统，需要时立即恢复原样；长时间聊天后对话窗也不会越用越占内存。可在 设置 → 功能 → 性能 里关闭。
- 浏览器顶栏、悬浮球、通知弹窗改用轻量界面，后台常驻时的内存占用大幅下降。

**扩展作者**

- 发布扩展时可以直接在发布弹窗里填「发布版本」，发布时自动更新扩展的版本信息文件，不用再去扩展目录手动改版本号；留空则按当前版本重提。

# v0.6.7 发布说明

**新增**

- 设置 → 常规里可以调整右下角提醒的停留时长（1–60 秒，默认 5 秒）。
- 扩展中心「我的扩展」说明区新增一键跳转，直接到安装「扩展开发 Skill」的位置。

**优化**

- 速达里点一个已经打开的应用或浏览器，会把它的窗口直接调到前台，不再重复打开一个。
- 收到托盘的应用（微信、QQ 这类）也能被唤出来；最小化的窗口点了会自动还原。
- 扩展安装前的权限提示更清楚：单独列出「用系统默认浏览器打开网页」，需要你确认的高风险能力也会一并提示。

**修复**

- 开着代理时，登录、检查更新、扩展市场和平台模型这些在线功能不会再连不上。
- AI 助手配置里，供应商卡片没填完（没有添加任何模型）时保存会被拦下并指出是哪张卡片，不会再出现「提示保存成功、内容却悄悄没了」。
- 网页链接打不开时会明确告诉你原因（比如扩展没有申请对应权限），不再点了没反应。

# v0.6.6 发布说明

**新增**

- **速记可以换着看了**：实时预览、左边写右边看的分屏、直接改源码，三种方式随时切，选过一次就会记住。
- **速达的资源能拖着排顺序了**：按住卡片一会儿把它拎起来，虚线框告诉你松手会落在哪，松手就保存。

**修复**

- **切到另一篇笔记时，刚敲的最后几个字不会再丢**——以前切得太快，还没保存的内容会没。
- **插件装好后点任何功能都提示加载失败的问题修好了**，尤其是自带一堆脚本、按老写法引用资源的插件。
- **拖动排序不会再把程序打开了**：以前想拖卡片排序，手一动就成了启动它。
- **拖动排序时整片网格不再跳动**，落点看得清楚。
- **插件不再被误拦自己的数据文件**：普通文件夹、压缩包和日志文件不再被一刀切挡住。
- **倒计时结束后留在原位变灰**，不再单独挂在列表最下面，顺序更稳定。
- **倒计时卡片会把内容撑满**：底部不再留一截空白，调整卡片高度时里面的条目自动跟着变。
- **分屏预览里的网址可以正常点了**——以前会变成点不动的纯文本。

**安全**

- **每个插件现在运行在各自独立的环境里**，插件之间、插件与你的数据之间互相看不到。
- **插件的本地后台程序需要你确认信任当前版本才会启动**；关掉联网权限会立刻把它停掉。
- **打开外部链接需要单独的「系统」权限**，带联网权限的插件不能再随便往浏览器弹网页。
- **自定义 AI 服务地址只允许加密连接**（本机地址除外），密钥不会被明文发出去。

# v0.6.5 发布说明

**新增**

- **待办可以重复了**：每天、每周、每月、每年、每个工作日，或者自定义「每几天、每几周」，还能设结束条件（到某天为止、共做几次）。勾一下就算做完这一轮，日期自动滚到下一次。
- **给待办打标签、把重要的置顶**：标签可以按类筛选，置顶的待办固定排在最上面。
- **侧边栏多了「待办」页面**：左边列表、右边日历，一眼看清哪几天有安排；点日历空白处直接新建，双击待办就能改。
- **工作台新增「日历」卡片**：用圆点标出每天有几件事，周期待办未来的日子也会显示出来。
- **待办能写说明了**（最多 200 字）：加粗、列表这类简单排版都认；写完不用点开，鼠标停在待办上就能看到。

**优化**

- **日历里的待办可以直接拖到别的日期**改期，提醒会保持原来的提前量。
- **待办列表支持按住拖动排序**；删除后还能点「撤销」把条目找回来。
- **该确认的地方都会先问一句**：勾选一个还有子项没做完的待办、删除标签之前，都会先跟你确认。
- 点开「待办」页面不再先白一下。

**修复**

- **在「待办」页面新建的待办不再一保存就看不见**（以前要切到「全部」才找得到）。
- **电脑一直开着跨过午夜后，日期会跟着走**：待办的分组、日历上的「今天」都会更新，工作台日历也不会再停在打开那天。
- **周期待办逾期很久后再勾选完成，日期会正确跳到下一次**，不再把重复设置弄丢。
- **撤销删除会把待办原来的说明、置顶、标签、重复设置一起找回来**（以前只找回标题和日期，重复设置会悄悄消失）。
- **置顶的待办在任何时间范围下都留在最上面**：选「今天」「本周」时不再整条消失。
- **改完重复规则、或勾掉一轮之后，日历上的重复条目会立刻更新**（以前要翻到别的月份才刷新，还会和新条目重影）。
- **新建重复待办时会提醒你先设一个截止时间**，不会再出现建好却勾不动、日历里也不显示的情况。
- 待办里的日期时间选择更准了（提醒时间不再被悄悄改掉几秒），编辑待办时弹出的日历样式也恢复正常。
- **修好了 AI 回复里可能被滥用的安全隐患**：回复里夹带的可疑内容现在会被过滤掉，不会在你电脑上偷偷执行操作。
- 部分电脑上点开扩展时闪一下黑窗的问题修好了。

**需要注意**

- 工作台原来的「整月日历」已并入新的「日历」卡片。升级后如果原来放日历的那格变成了时钟，从模块库里把「日历」拖回来即可。

# v0.6.4 发布说明

**新增**

- **桌面上的扩展卡片也有标题了**：以前只有内置卡片带标题，现在扩展卡片顶部同样有一行「图标 + 名称」。嫌名字不合适，可以在布局编辑器里点卡片上的「Aa」改成自己习惯的叫法，也能把这一行收起来，给内容多留点地方。
- **悬浮球转不转，自己定**：设置 → 悬浮球里新增「静止时保持转动」——想要它一直转就打开，想省电就保持关闭（默认关）。

**优化**

- **挂后台不再一直吃电**：窗口收进托盘、或者卡片看不到的时候，悬浮球、系统资源、速达和扩展相关的刷新都会停下来。合上盖子接着用，笔记本不容易发烫了。

**修复**

- **矮卡片里的扩展内容不再缺一截**：桌面卡片比较小时，扩展页面底部会贴边、最下面一行被切掉，现在能完整显示，下边距也回来了。
- **悬浮球打开「静止时保持转动」后真的会转了**：之前这个开关打开没反应，现在已经修好。

# v0.6.3 发布说明

**新增**

- **扩展里的链接能在浏览器里打开了**——以前点热搜、点扩展里的外部链接，常常点了没反应，现在会用你的默认浏览器打开

**修复**

- **卸掉自己发布的扩展后，扩展市场不再错把它当成「已安装」**——以前会出现市场里显示已安装、版本号还是旧的，想装却装不了
- **小格子里的卡片显示更合理**——卡片很小时不再把标题挤成两行占地方，标题条不占高度、功能按钮悬停时才出现
- **修好了部分电脑上本机扩展的后端起不来的问题**

**优化**

- **工作台卡片能自己改标题了**——不想看某个卡片的名字可以隐藏，也可以换成你习惯的叫法
- **小格子的排版不再打架**——卡片最小尺寸放宽了，格子变大内容自动多排，排不开时可以滚动，不会互相盖住
- **扩展详情弹窗的四边间距统一了**，看着不再别扭

# v0.6.2 发布说明

- **邮箱验证码登录开通了**——以前只能用 GitHub 登录平台账号，家里网络访问 GitHub 不稳定时，平台免费额度、发布扩展这些功能会一起用不了。现在登录区多了一个「用邮箱验证码登录」：填邮箱 → 收到 6 位验证码（10 分钟内有效）→ 输入即可登录，全程不经过国外网络
- **发码有节制，出问题也说清楚**——同一个邮箱一小时内最多收到 5 封，发送成功后 60 秒内不能重复发（避免你刚收到的那封被下一封顶掉）；如果发得太频繁、服务器发信失败，界面会直接说明原因和下一步该怎么办，不再是让人看不懂的报错

# v0.6.1 发布说明

- **登录、AI 对话和扩展下载全程加密了**——之前这些请求是明文发出去的（域名还没有证书）。现在全站走 HTTPS，中途的网络设备看不到你的账号凭证与聊天内容，也看不到你在下载什么扩展
- **扩展市场「市场源异常 404」彻底修好**——部分电脑升级后市场一直拉不出列表，这版把市场与更新的地址统一收到平台服务端，并自动修正旧设置里残留的失效地址，升级后自愈，不用手动改配置
- **扩展与更新包的下载更稳**——扩展包、图标、截图和升级包改由平台统一分发（自带 5 分钟缓存，服务端故障时回落到上次的列表），对象存储地址不再出现在你的电脑上
- **平台免费额度改成一个开关**——设置里登录账号后打开开关即可使用，不再需要添加模型或填写任何内容；AI 对话的模型列表里显示为「x-hub 平台」，平台有多个模型时自动轮流使用，不用手动挑

# v0.6.0 发布说明

- **本机扩展调试：加上源码目录就能边改边看**——扩展中心新增「我的扩展」页：选一个扩展源码文件夹加进来，它立刻能用（不会混进「已安装」列表），改完 HTML / CSS / JS 保存就自动刷新，不用重新打包安装；想上架再从那里发布
- **让 AI 助手帮你写扩展**——设置里新增 Skills：一键把「扩展开发技能包」装进你电脑上的 AI 编程助手（Claude Code / Codex 等）的目录，之后对助手说「用 x-hub-extension 给我做一个 XX 扩展」，它就会按 x-hub 的规范生成扩展；装了以后新版会提示更新，也能一键卸载
- **应用内发布扩展**——扩展开发者可以直接在应用里发布：先做一次本地预检（清单格式、申报权限、目录结构），再打包上传到平台；提交后能看到审核结论，审核期间或之后还可以撤回，不用再手工跑脚本
- **平台账号与免费额度**——支持 GitHub / 邮箱登录平台账号。登录后在 AI 供应商设置里可以一键添加「平台免费额度」模型，与自备 Key 的供应商并存；账号凭证存在系统钥匙串里，不明文落盘
- **安装扩展前先看清权限和截图**——市场详情页会列出该扩展申请的权限，并按风险分级用大白话说明（联网、读写数据、执行外部程序等危险能力单独提醒）；截图区以主图 + 缩略图切换展示，可左右翻页、点开看大图
- **已下架的扩展会明确警示**——某个版本被下架后，扩展中心和市场都会明确提示；不会静默卸载或禁用你已经装好的扩展，也不会再引导你去安装被撤销的版本
- **扩展再也碰不到你的本地数据**——扩展运行环境和宿主数据彻底分开：扩展页面改用独立来源，无法再读取本地数据库、配置和日志；能访问的目录也收窄到扩展、图标、壁纸、剪贴板图片这几处
- **设置页打开不再空白，还能直接搜**——设置页重做为按需加载：打开时先显示占位骨架并后台预热，不再有短暂空白；顶部新增设置项搜索，输入关键词直接跳到对应设置
- **修好浮窗在任务栏多出按钮**——悬浮球、便签、倒计时、剪贴板、通知等浮窗隐藏后再显示时，任务栏会多出一颗按钮；这一版统一修复
- **改设置不会再悄悄弄丢你的选择**——修复「点过『跳过此版本』，之后改任意设置又被重新提示」和「刚添加的本机扩展目录在改其它设置后凭空消失」：这类由应用自己维护的设置在保存别的设置时不再被覆盖
- **服务端地址改为内置**——平台相关功能改用内置服务端地址，不再读取设置里的旧地址，也不需要手动配置

# v0.5.5 发布说明

- **修好开启对话小窗后整个应用卡死的问题**——上一版在设置里开启「以独立窗口打开 AI 对话」时，应用可能整体卡住：对话窗打不开、其他功能不再响应、连退出都点不动，只能强制结束进程；这个版本彻底修复，并把对话小窗改为一打开应用就默默准备好，切换开关不再有风险
- **待办浮窗支持子待办**——屏幕上的待办小窗现在和工作台待办卡一样：能看到每条待办下面的子待办，可以勾选、添加、删除、折叠展开，双击文字直接修改
- **勾完父待办，子待办自动跟上**——在工作台把一条带子待办的待办标记完成时，它下面没完成的子待办会一并勾上；取消完成则不会动子待办，各自的进度保留
- **子待办可以拖动排序了**——同一条待办下的子待办支持按住上下拖动调整顺序，拖动时有位置指示线
- **待办浮窗可以调大小**——把鼠标移到待办小窗的边缘或角落，可以拖动改变窗口大小（不会小到放不下内容）
- **修好深色主题下剪贴板浮层发白**——深色主题下剪贴板历史浮层、右键菜单和提示气泡的背景显示成浅色，现在恢复深色

# v0.5.4 发布说明

- **AI 对话可以独立小窗打开**——设置 → AI 助手新增「以独立窗口打开 AI 对话」开关：开启后对话变成一个可以随意拖动、缩放、置顶的小窗，关掉只是收起，位置和大小都会记住；标题栏按钮、Ctrl+Shift+K 快捷键和悬浮球的「AI 对话」入口都会跟着这个设置走，两种形态随时切换
- **对话统计更易读**——输入框上方的用量统计改成 k、M 缩写（如 12.5k、1.2M），生成速度直接显示「tok/s」，一眼看清输入/输出/缓存/速度
- **修好提醒弹窗不出现**——修复部分电脑上右下角提醒弹窗（倒计时/待办到点）整批不出现的问题；应用刚启动头几秒触发的提醒偶尔会漏掉，现在也不会了
- **界面细节顺手修**——把对话小窗拖到一半松手再划过标题栏不会再莫名跟着窗口跑；设置里的「独立窗口」开关切换失败时会提示重试，不再出现界面显示已切换、重启后又变回去的情况

# v0.5.3 发布说明

- **右下角通知弹窗**——倒计时到点与待办提醒改为自绘的右下角通知卡片：从右侧滑入、多张自动堆叠、约 5 秒自动淡出、点卡片或 × 可立即关闭，深浅主题跟随；不再走 Windows 系统通知中心，彻底修复 Win10 收不到弹窗的问题（Win10/11 表现一致），通知不抢当前输入焦点，卡片离任务栏与屏幕右沿各 16px
- **开机自启自愈**——程序启动时自动校验注册表自启项：被系统禁用或指向旧版本路径时自动修复；设置页在自启实际失效时给出提醒并支持一键"重新启用"
- **剪贴板历史滚动加载**——修复右上角徽标显示 300+ 条但列表往下滑只有 50 条的问题：列表原本固定只拉首页 50 条且无续拉逻辑，现在滚动到底自动按页续拉（每页 50 条，按 id 去重防止分页期间新复制造成的错位重复），↑↓ 键盘选中触底同样续拉；搜索态徽标在结果超过一页时显示「N+ 条」
- **托盘/重启图标残留修复**——右键托盘「退出」与自动更新重启后，任务栏托盘不再残留需要鼠标悬停才消失的幽灵图标（两条硬退出路径补发 Shell_NotifyIcon 删除通知）
- **悬浮球贴边交互修复**——拖到屏幕边缘松手现在能正确吸附成半隐待机，鼠标挪上去完整弹出（球体与粒子环绕）、移开自动缩回；拖到屏幕中间任意位置松手就停在那里，不再弹回；手速快把球整个甩出屏幕外，一秒内自动弹回（此前会"点不到也拖不回"）；贴边待机的球不再只露一条弧（多露出约三分之二）；吸附只在真正贴边（约一个球直径内）时触发，放下球不会被莫名挪走，消除了松手时的抖动感

# v0.5.2 发布说明

- **工作台形态化布局**——时钟卡四形态（大时钟 / 今日阴阳历 / 整月日历 / 极简时间）、天气独立卡片（简版 / 详情版）；布局编辑器所见即所得重构：clock/weather 画布内挂载真实卡片、其余模块走真实卡片结构的等比缩印预览，支持形态切换、最小尺寸钳制与适配徽标；时钟卡农历 / 月视图派生值改分钟粒度驱动，秒级 tick 不再触发每秒重算；新增农历转换（utils/lunar.ts，1900–2100）与布局编辑器模块预览组件（components/DashModulePreview.vue）
- **扩展宿主数据访问口子**——新增 data.* 桥能力（笔记 / 待办 / 便签 / 浮窗便签 / 速达 / 提示词 / 标签，按 data:read / data:write 权限读写），扩展桥补齐 data 命名空间封装与 xhub.call 通用调用通道；todos / stickies / detached_stickies 增加 version 乐观锁（expectedVersion 冲突检测），主 UI 本地写同步推进版本链，为局域网同步预留冲突检测基础
- **service 对外监听**——扩展后端支持绑 0.0.0.0 / 局域网地址（network 权限门控），探活按监听地址探测并移出命令线程（打开扩展不再卡顿）；防火墙规则先删后加幂等、按程序定向、停止 / 卸载 / 启动失败时同步清理
- **审查修复**——扩展桥入参 fail-fast 校验（expectedVersion / dueAt / remindAt / parentId / tagIds 非整数报错、标题非空）；NOT_FOUND 与 CONFLICT 错误区分、乐观锁删除返回级联子 id、repo update 系补 NOT_FOUND 语义；布局编辑器预览 XSS 转义、老布局放大重叠消解、形态钳制右边界修正、无效 variant 归一与形态广播补发；农历超出 2100 年优雅返回空
- **布局编辑器预览重做**——「样式化预览」（utils/dashPreviews.ts）退役并连带移除 v-html 注入面，改由 components/DashModulePreview.vue + composables/useDashPreviewData.ts 承担：预览逐张复刻真实卡片的 DOM 结构与设计令牌，尺寸写成「真实 px × 缩印系数 `--dp-k`」（画布每列像素 ÷ 真实工作台每列像素，随窗口缩放与侧栏开合实测重算），数据取 store 真值且过滤 / 排序口径逐条对齐真卡（待办分组计数取全量、倒计时按 end_at 升序、速记最近一条按 updated_at 降序、最近使用按启动时间倒序、相对时间与摘要沿用概览卡实现），编辑器在场时按分钟推进时间 / 农历 / 倒计时剩余；形态浮层缩略图按该形态真实宽高比成形（月历方形、通栏扁条不再被压进统一小框）；画布列间距与卡片圆角与真实工作台取齐（16px / 12px），四色适配描边收进 hover 与「填充审计」开关（红色「低于最小」仍常显）

# v0.5.1 发布说明

- **分发端点迁移：腾讯云 COS 替代 Cloudflare R2**——客户端默认市场/升级清单端点切到 COS（广州），国内下载更快更稳；CI 扩展发布通道同步切换（rclone TencentCOS）并新增 registry.json(.sig) 可访问性校验步骤；upload-market / upload-release 脚本三通道化（cos 主通道 / r2 过渡兜底 / sftp 自建 Nginx 备选），publish 脚本端点改由 XHUB_DIST_BASE_URL 环境变量驱动，产物目录 dist/market、dist/release → dist-market、dist-release；新增 docs/self-hosted-distribution.md（迁移总方案与六阶段切换）、scripts/sync-r2-to-cos.ps1（阶段 0 R2→COS 一键平移）、scripts/server/（自建 Nginx 一键初始化与站点配置）
- **修复升级/重启链路**——新增 updater::relaunch_app：拉起新进程前先释放 single-instance 互斥与隐藏窗口（tauri_plugin_single_instance::destroy）并停掉 service 后端子进程，修复「升级自替换后 / 点立即重启后应用不自动重启（新实例被误判第二实例自杀退出）」的历史问题；restart_app 从 app.restart() 统一改走 relaunch_app（规避非主线程 restart 被 Exit 分支截杀只退出不重启）
- **悬浮球环形菜单收紧**——菜单窗口 312 → 260，按钮轨道半径 118 → 92（按钮内沿距球缘 45px → 19px），装饰双轨道环同步缩小
- **待办子待办折叠/展开**——父待办行内新增折叠/展开按钮（默认展开、不持久化，折叠态图标常驻可见），折叠时隐藏子列表，点「+」添加子待办时自动展开
- **版本同步**——package.json / tauri.conf.json / Cargo.toml / README 徽章 / AGENTS.md 元信息统一升至 0.5.1

# v0.5.0 发布说明

- **桌面悬浮球（ADR 0004）**：主窗口隐藏/最小化时桌面常驻悬浮球——单击展开环形快捷菜单（默认 6 键：工作台/速记/速达/全局搜索/剪贴板/设置，设置页可增删排序、最多 8 个），双击显示主窗口，右键托盘同款菜单，Esc/失焦/点击空白/再点球体收起；全息粒子球视觉（canvas 粒子云 + 3D 陀螺环），配色跟随主题强调色并实时跟随主题切换；支持拖拽、屏幕边缘完整贴边吸附（球不滑出屏）、位置记忆；窗口启动期预创建隐藏常驻，开关只切显隐（规避运行时创建/销毁 WebView2 窗口导致整窗卡死）
- **扩展 fs 保存能力**：桥 API 新增 `fs.saveText / fs.saveFile / fs.saveAs`（需 `fs` 权限），扩展可把文本/二进制内容保存到系统下载目录——文件名清洗（防路径穿越、剔 Windows 非法字符、保留设备名回退）、64MB 上限、重名自动追加序号，saveAs 弹系统「另存为」对话框由用户选路径；修复扩展页面网页级下载被 WebView2 宿主拦截而静默失效的问题
- **剪贴板浮层改为启动预创建隐藏常驻**：唤起/收起只做 ShowWindow 级快操作，移除 30s 延迟回收销毁逻辑；修复从悬浮球唤起剪贴板浮层时与窗口操作交错导致的整窗未响应卡死；浮层可见性判定改用 Win32 IsWindowVisible（WebView2 子窗口上 `is_visible()` 误报导致 toggle 误入收起分支）
- **壁纸简化为单套**：移除亮/暗主题独立壁纸配置，所有主题模式共用同一张壁纸与蒙版（设置页移除主题页签）；壁纸目录清理按唯一引用判断
- **退出可靠性**：service 后端子进程 kill 后有界回收（轮询至多 1s，防异常子进程把退出流程拖死）；进程退出事件清理完成后直接结束进程，托盘「退出」100% 生效
- **配置保护**：save_config 对悬浮球字段（开关/吸附/同显/按钮/位置）一律以磁盘为准合并，防止主窗旧快照把拖拽后的位置/设置覆盖回去
- **扩展权限可读化**：扩展设置弹窗的权限列表改为用户可读说明（数据/文件/剪贴板/网络/系统/通知/事件/共享存储），原始权限标识悬停可见
- **测试与文档**：新增文件名清洗（路径穿越/非法字符/保留设备名）、保存路径去重、fs 能力权限声明等单元测试；ADR 0004 标记已实施并补记「预创建 + 显隐联动」修订，AGENTS.md 新增浮窗生命周期铁律（约定 41）
- **悬浮球交互打磨**：双击主窗口现为显隐切换（开着则收起、隐藏则显示聚焦），不再只能呼出；拖拽吸附触发距离放宽至约一个球宽（球缘距屏边 96px 内松手即明显吸到贴边位），吸附开/关差异一目了然
- **悬浮球/倒计时透底态可读性**：环形菜单按钮改主题强调色烟玻璃实底 + 悬停提亮；壁纸透底态下倒计时类型 icon/标签加深色玻璃覆盖与浅墨文字（对齐 toast 处理）
- **速记细节**：从悬浮球等非速记视图新建笔记后自动切到速记页，不再「只建了笔记却停在原视图」

# v0.4.3 发布说明

- **待办拖拽排序**：顶级待办支持组内上下拖动（指针实现，规避 Tauri 原生拖放拦截），插入线指示落点，松手后整组顺序写入 `todos.sort_order` 持久化；待办浮窗同一排序规则；修改截止日期换组自动回到默认排序，新建条目在手动排序过的组内补置顶位
- **亮/暗主题独立壁纸**：外观设置新增「亮色主题 / 暗色主题」页签，两主题可分别设置壁纸与蒙版；暗色未单独设置时跟随亮色（老配置观感不变）；壁纸目录改为按当前配置引用清理，更换/清除壁纸不再整目录删除
- **Rust 单元测试包装脚本**：新增 `npm run tauri:test`——`cargo test --no-run` 后用 SDK mt.exe 给测试 exe 嵌入 Common-Controls v6 manifest（`src-tauri/windows/app.manifest`），修复 Windows 下测试进程启动即死（STATUS_ENTRYPOINT_NOT_FOUND）
- **速记表情**：笔记编辑器斜杠菜单新增「表情」分组（最近使用优先、系统缺字自动跳过），「更多表情…」打开完整选择器（分类 / 搜索 / 最近使用，localStorage 持久化）
- **速记编辑体验**：点击编辑区空白处光标落到文末（点在内容上原地定位）；编辑区内边距收紧、块把手悬停偏移对齐，不再翻转盖字或触发横向滚动
- **时钟秒级显示**：时钟卡显示秒，定时器对齐下一秒边界触发，消除相位漂移导致的跳秒
- **壁纸透底态可读性**：透底态描边墨阶同步翻白墨（修复照片上待办复选框呈「黑圈」），复选框圈环提亮一档并加暗晕，明暗照片下都可见
- **审查修复**：斜杠菜单插入表情的指令定位公式错误（替换后 `/指令` 残留）；表情选择器搜索结果中跨分类重复 emoji 导致 v-for key 冲突；待办拖拽监听在快速甩动下泄漏并可能于后续按下时误触发拖拽

# v0.4.2 发布说明

- **修复「点了立即重启还是旧版」**：更新自替换成功后立即以新 exe 拉起子进程接管启动、当前进程退出（此前当前进程镜像已被改名为 .old，原地继续跑仍是旧版本）；拉起失败时自动兜底为下次启动生效；每次启动顺手清理上次升级残留的 .old 文件，exe 目录不再长期躺着旧镜像
- **老库升级回归测试**：新增 v0.3.3 → 最新版直升路径测试——迁移连跑两次幂等、老数据无损、业务层读写正常、父子待办级联删除生效
- **文档**：新增 ADR 0004 桌面悬浮球（导航枢纽 + 环形快捷菜单）决策记录与可交互原型；CONTEXT.md 预登记悬浮球相关术语；R2 分发文档同步自替换新流程

# v0.4.1 发布说明

- **待办列表更紧凑**：日期/提醒/子待办进度徽标内联到标题行尾，不再固定占第二行，单行任务行高约为原来一半；标题过长时徽标整组自动换行兜底；「日期」占位按钮改为悬浮/聚焦时出现；徽标胶囊与行间距收紧
- **首屏加载优化**：主包 617KB→328KB——设置、全局搜索、聊天面板、扩展中心/扩展视图、布局编辑器、提示词管理等低频界面改为按需异步加载；速记编辑器依赖拆分为独立懒加载 chunk（Milkdown/ProseMirror 1224KB + KaTeX 252KB），随应用长期缓存
- **工程化**：新增 `.gitattributes` 统一文本文件 LF，消除 Windows 下 CRLF/LF 状态噪音；vite 构建改用 rolldown `codeSplitting` 分组并放宽懒加载 vendor chunk 告警阈值

# v0.4.0 发布说明

- **待办支持截止日期与提醒**：行内徽标区分逾期（红）/今天（橙）/明天（品牌色）/更远日期（灰），点击弹出排期层——快捷项（今天/明天/下周一）+ 日历 + 时间选择，可独立开启到点提醒；待办列表按「逾期 → 今天 → 有日期 → 无日期」分组，仅设日期的截止按当天 23:59 计
- **待办支持子待办**：父条目下缩进展示，行尾「+」添加，父条目显示 n/m 完成进度条；删除父待办级联删除子待办并支持一键撤销（恢复时保留原创建时间、优先级、排期与完成状态）；浮窗与待办概览卡只统计顶级待办
- **待办提醒后台化**：新增 Rust 后台线程到点扫描提醒（应用关闭/休眠期间错过的提醒不补发），到点弹系统通知 + 应用内 Toast；新增 schedule_todo 命令
- **系统通知通道重做（真 Toast）**：Win11 24H2+ 不再把托盘气泡转成系统通知，通知改走 Windows Toast（右下角横幅 + 操作中心）；便携版首次发通知时自动注册应用 AUMID（用户级注册表，免安装器），应用名与图标正确显示；倒计时提醒同通道受益
- **老库升级修复**：待办新列迁移后统一建索引，修复旧数据库升级时因「no such column: parent_id」导致的启动崩溃
- **待办输入交互调整**：新增输入框改为一行高，回车新建（粘贴序号列表自动拆成多条）；编辑待办时回车保存、Shift+回车换行；子待办复选框与父待办首字对齐

# v0.3.3 发布说明

## 🐛 修复

- **修复速记编辑器选中工具栏、块把手、链接气泡图标在亮色主题下白叠白不可见**：图标色此前映射到白色描边令牌，白图标叠在近白工具栏上几乎隐形；现改映射中性灰墨（暗色同步微调为更易读的浅灰）
- **修复壁纸态侧栏/标题栏图标带白色光晕描边**：白晕叠在壁纸上呈粉笔白框，现已去掉；暗色与透底白墨态保留黑色光晕，保证浅色图标在浅色照片上仍可辨认
- **修复应用自动更新在慢网络下反复失败**：下载支持断点续传（中断后从已下载字节处继续，不再从零开始），失败自动退避重试（上限 8 次）；下载加互斥防并发互踩，超时策略改为连接 15s + 空闲读 30s；续传偏移越界（416）自动丢弃残片重下

## 🎨 界面优化

- **壁纸 + 透明模式下卡片去白边**：卡片白色描边与顶部白高光在壁纸上呈粉笔白框，现收敛为透明只留落影定界，亮色主题补一档中性落影保住卡片层次；无壁纸/不透底形态白边照旧
- **透底（白墨）模式下编辑器浮层换深玻璃实底**：选中工具栏、斜杠菜单、链接气泡、图片说明改为深底白图标，不再被壁纸亮部冲淡；图片上传按钮、代码搜索面板配色一并统一
- **更新弹窗体验**：下载中关闭弹窗后再打开，直接回到下载进度视图（此前显示为灰色按钮且无任何进度反馈）

---

# v0.3.2 发布说明

## 🐛 修复

- **修复自动更新在「程序与数据目录不在同一磁盘分区」时升级失败**：自替换用 rename 移动新 exe，跨盘符时报「os error 17 系统无法将文件移到不同的磁盘驱动器」并回滚，表现为重启后仍是旧版本、更新弹窗反复出现；现 rename 失败自动退化为复制，跨盘安装可正常完成
- **新增防降级守卫**：启动时若待应用更新版本不高于当前版本（如已手动安装更高版本），自动清理过期更新包与待应用标记，防止旧包把新装的 exe 又覆盖回旧版

## 📌 说明

- 程序 exe 与数据目录不在同一磁盘分区的存量用户，本次升级需**手动下载 zip 覆盖安装一次**（自动更新由旧版本执行、无法跨盘替换自己）；之后自动更新即恢复正常。exe 与数据目录同盘（默认部署）的用户不受影响，可直接收到本次更新

---

# v0.3.1 发布说明

## ✨ 新功能

### 速记 · Milkdown Crepe 编辑器
- **所见即所得**：编辑器迁移至 Milkdown Crepe，Markdown 为真相源，保留 600ms 防抖自动保存；编辑器异步分包加载、不拖慢启动，标签行同步迁入
- **笔记图片三路入口**：粘贴 / 拖拽 / 点击上传均可插图，按内容哈希落盘（同图自动去重），经 `xhub-note` 协议渲染——文件名严格校验防路径穿越，迁移数据目录后链接仍有效；浏览器预览无后端时回退 data URL
- **块拖拽改为指针实现**：六点把手拖动带插入线指示，单事务搬移顶层块（一个撤销步骤即可回退）；不再依赖 HTML5 DnD，与窗口原生文件拖放互不干扰，速达拖入导入不受影响

## 🐛 修复

- 修复异步挂载期间快速切换笔记时挂载请求被静默吞掉的竞态：编辑器停留旧笔记内容、防抖保存跨笔记污染
- 笔记列表摘要与全局搜索片段不再显示 Markdown 语法符号（统一纯文本化）；标题为默认值（空 /「无标题笔记」）时自动从正文首行派生

## 🎨 界面优化

- 壁纸透底新增「白墨可读性」形态：文字 / 图标转白墨、表面令牌翻转为深玻璃，输入框边界与蒙版同步打磨，强透底壁纸上依然清晰可读
- 倒计时模式图标与徽章底色改以实心基色混色，避免壁纸透底压暗深色文字
- 扩展桥广播壁纸状态令牌（`themeTokens.wallpaper`）：玻璃透明度 / 沉浸模式变化时实时同步给扩展 iframe
- 笔记图片上传改为前端预检 10MB 上限再走 IPC，超限即时提示；顺带移除未使用的 useDarkTheme composable

---

# v0.3.0 发布说明

## ✨ 新功能

### 应用自动更新
- **自研升级链路**：`releases/update.json` 升级清单 + Ed25519 分离签名（`.sig` 并列上传，客户端内嵌公钥验签，验签通过才信任清单）→ semver 版本比较 + `minimumUpgradable` 跳级保护 → 平台匹配（标准版 / 便携版分别取 `url` / `portableUrl`）
- **下载与自替换**：流式下载边下边算 sha256（由验签通过的清单背书）→ 写待应用标记 → 重启时解包并「exe → exe.old / 新 exe → exe」两步 rename 自替换，任一步失败自动回滚、下次启动重试
- **自动检查**：启动 5 秒后静默检查一次，之后按配置间隔（默认 4 小时，配置 `update_interval_hours`）循环；检查失败静默记日志不打扰；设置 → 关于「自动检查更新」开关可关
- **交互闭环**：发现新版本弹出全局更新弹窗（版本号 / 说明 / 大小 / 便携版标记），支持「跳过此版本」（记录到配置，自动检查不再提示该版本）/「立即更新」（进度条 + 错误展示）/ 就绪后「立即重启」；About 页「检查更新」手动触发（忽略跳过记录，可再次看到该版本）

### 扩展系统
- **扩展中心**：本地清单展示已安装扩展，支持从 GitHub 仓库地址下载 zip 打包扩展并解包安装、卸载
- **扩展注册表**：manifest 解析 + 本地安装记录（含权限声明、版本）
- **多种形态**：module 卡片（嵌入主界面）、view 页面、window / drawer 浮窗形态
- **桥 API（window.xhub）**：扩展通过宿主注入的桥与主程序交互（窗口、状态、服务调用等），权限按 manifest 逐项授权
- **service 托管**：内置运行时按需下载、自动降级；运行时策略（自动 / 始终内置 / 始终系统）可配置
- **双向通道**：`/svc/*` HTTP 反向代理（统一 CORS）与 WebSocket 升级双向隧道

### 扩展：侧栏固定与打开方式
- 扩展设置新增「侧边栏」区块：可把扩展**固定到左侧栏**，点击即打开；并可在**视图 / 窗口 / 抽屉**三种打开方式中选择，选择后从侧栏等入口按该方式打开
- 侧栏扩展入口统一应用「应用图标」质感（中性软底 + 细描边），与宿主导航图标风格一致；固定文案字体与设置面板统一
- 扩展视图顶部工具条精简：去掉「← 扩展中心」返回按钮，显示扩展名 + 快捷「在窗口打开 / 在抽屉打开」

## 🐛 修复

- 扩展运行时策略设置项移入独立「扩展」分类，与扩展中心统一管理
- 修复扩展「在窗口打开」失败：窗口 label 使用扩展 ID 含点号触发 Tauri label 校验报错 → 改为 base64url 编码 label；建窗命令改为 async 避免与主线程互锁死锁
- 修复便携版扩展安装到用户目录：扩展根目录、市场清单、内置 Node 缓存改用 `paths::data_root()`（便携版跟随 `exe\data`），与其余数据一致
- **修复更新弹窗「立即更新」按钮无响应**：`@click="void onUpdateNow"` 是 Vue 模板表达式陷阱——`void` 一元操作符作用于函数引用时只丢弃不调用，点击永远无效果；改为 `@click="onUpdateNow"`（方法名自动调用，与「取消 / 跳过此版本 / 立即重启」一致）
- **修复升级后重启不自替换**：待应用标记 `.pending.json` 以 camelCase 写入 `zipPath`，但读取结构体 `PendingUpdate` 缺 `#[serde(rename_all = "camelCase")]`，路径反序列化为空 → 启动时误判「待应用更新包不存在」直接清理标记，自替换从未执行；已补齐 serde 映射并加单元测试防回归

---

# v0.2.3 发布说明

- 提示词浮窗、待办浮窗新增「置顶 / 取消置顶」切换按钮
- 提示词 / 待办浮窗首次弹出时定位到主窗口中心附近
- 待办支持粘贴「1. 2. 3.」序号列表一次拆成多条待办
- 待办输入框改为随内容自动增高的多行输入框（封顶 5 行）
- 修复中文输入法选词时按回车误提交待办的问题
- 修复提示词管理弹窗新增按钮位置文案、以及内容输入框未撑满宽度的问题
- 倒计时卡片：进行中 / 已结束列表间距优化、条目图标与圆底对比增强、条目轻阴影区分层级

---

# v0.2.2 发布说明

## ✨ 新功能

### 数据存储路径与便携版
- **数据存储路径**：设置 → 数据 → 数据存储路径，默认显示用户级目录 `%APPDATA%\x-hub`；可「更改」迁移到任意目录，确认后自动迁移数据并重启，重启后读取新目录数据（旧目录保留）
- **便携版**：exe 同目录放一个空文件 `portable` 即可启用便携模式，数据固定跟随 `exe\data` 子目录，整个文件夹拷到 U 盘即可随身携带、换电脑即插即用；便携版不支持更改数据路径
- **双形态发布**：同一份 exe 打包为两个 zip——标准版（纯 exe，数据在用户级目录）+ 便携版（exe + `portable` 标志，数据跟随程序目录），命名以 `-portable` 后缀区分

### 数据备份 / 恢复改为压缩包
- 备份不再散落 `app.db` + `icons` 目录，改为打包成单个压缩包 `x-hub-backup-时间戳.zip`（多次备份互不覆盖）；恢复时选择该压缩包即可，解压暂存后重启生效

### AI 对话
- Markdown 回复渲染加入缓存：每条消息只解析一次，历史消息多时不再反复全量重渲染，滚动与输入更跟手；切换会话时自动清空缓存

## 🐛 修复

- 修复便携版被标准版残留引导文件干扰、误读用户级旧数据的问题（改为便携标志优先，数据固定跟随 exe）
- 修复便携版更改路径后数据位置标签误报、以及绝对路径跨电脑/盘符失效的问题（收敛为便携版禁用改路径）

---

# v0.2.1 发布说明

## ✨ 新功能

### 工作台自定义布局
- **自由拖拽布局**：工作台从固定三列 Bento 升级为 12 列 × 15 行整数格棋盘，模块可自由拖拽换位、右下角缩放（最小 2×2），内容严格占格、不留白
- **两栏编辑器**：设置 → 工作台 →「自定义布局」打开编辑器——左侧模块库（12 个模块，含原「中上可切换」的 Token 统计 / 速记概览 / 待办概览 / 速达数量 / 倒计时，全部拆成独立模块），右侧 12×15 等比缩放预览，完成后「回到主页面」
- **智能落位**：拖入 / 拖动到已占位置时自动向下找最近空位、不再弹回；拖动带 6px 阈值（点击卡片不误移动），目标位置实时显示虚线占位框、松手才吸附
- **推荐布局**：内置 12×15 八模块推荐模板（时钟 / 倒计时 / 待办 / 系统资源 / 提示词 / 便签 ×2 / 最近使用），一键套用
- **布局持久化**：保存至本地，重启自动还原；提供「清空 / 推荐布局」快捷操作

### 倒计时卡片交互优化
- 每个倒计时条目的「暂停 / 浮窗 / 删除」按钮默认隐藏，鼠标悬停条目时浮现，条目左侧的名称与剩余时间完整展示、更清爽

### 设置页整理
- 分类从 9 个整理为 8 个：删除「通用」，侧边栏展开归入「外观」，时钟语录与名言来源归入「工作台」，剪贴板呼出快捷键与全局快捷键合并到「快捷键」
- 剪贴板移除「记录图片」「记录文件」开关（默认始终记录，简化设置）
- 展示优化：「字体大小」「版本历史」折叠收起，剪贴板内部分组（保留策略 / 记录行为 / 操作）

## 🐛 修复

### 工作台布局编辑器
- 修复预览区行高被拉满、模块尺寸显示不实的问题（改为 12×15 均分填满、窗口缩放自适应）
- 修复模块拖到右侧 / 被占区域落不进去、直接弹回的问题
- 修复缩放后拖动仍按默认尺寸计算、导致放不进右侧空位的问题
- 修复点击卡片即误移动的问题（加入拖动阈值）
- 布局数据改为存入应用配置（不再用 localStorage，老数据自动迁移）
- 进入编辑器未点「确认」就切走，自动恢复编辑前的布局（草稿机制）

### 其他
- 全局禁止网页右键菜单（不再弹出浏览器上下文菜单）

---

# v0.1.19 发布说明

## ✨ 新功能

### 在线服务：天气、名言金句、联网探活与离线隐藏
- **实时天气（Open-Meteo）**：时钟卡片右上角展示当前城市温度 / 体感 / 湿度 / 风速 / 天气图标（WMO code → 中文 + 图标）；数据走后端 reqwest 拉取，保持「唯一通道 = Tauri invoke」约定
- **城市定位**：内置中国主要城市经纬度表（本地优先，避免 Open-Meteo 地理编码把中文城市名匹配到同名小地方）；支持输入城市名解析或**一键按 IP 定位**
- **在线名言金句（hitokoto）**：时钟卡片语录区接入在线金句，点击换一句；**离线自动回退本地内置语料**，无网也不空屏
- **联网探活与离线隐藏**：后台定时探测连通性，离线时在线功能自动隐藏 / 降级，恢复联网后自动回到在线状态
- **设置 → 联网**：新增「联网」区——联网总开关、城市设置（输入解析 / IP 定位 / 保存）、名言来源切换（在线 / 本地语料）

## 🐛 修复

### 工作台布局
- **便签卡片不再限高**：移除工作台便签区 `max-height: 440px` 上限，便签数量多时随内容自然撑开，避免拥挤截断

---

# v0.1.18 发布说明

## ✨ 新功能

### 剪贴板历史 v1.2：支持图片与文件
- **记录图片**：复制图片 / 截图自动落盘快照进历史（PNG 原样存 `.png`，位图补 BMP 文件头）；浮窗缩略图预览、点击放大，支持一键「保存图片」导出到指定路径
- **记录文件**：复制文件记录路径列表（CF_HDROP，不拷贝文件内容），浮窗展示文件数量，粘贴时写回系统剪贴板
- **文本 / 图片 / 文件三类型混排**：历史列表统一展示并按类型标注；相同图片（字节哈希）或相同文件列表自动去重、挪到最前
- **记录开关**：设置 → 数据 新增「记录图片」「记录文件」两个开关（默认开启），关闭后仅不记录新条目，历史保留
- **快照生命周期管理**：删除 / 清空 / 过期清理时联动删除磁盘图片快照，杜绝孤儿文件泄漏

### AI 对话性能优化
- **流式推送攒批**：后端增量按「≥64 字符或 ≥40ms」合并推送，避免长回复几千个 1~4 字小 chunk 逐个走 IPC 拖慢渲染；首段立即推，保住首字延迟手感
- **流式渲染节流**：前端 Markdown 按 ~80ms 节流重渲染，杜绝长回复 O(n²) 重解析卡死主线程
- **上下文窗口**：长对话只取最近一段作为模型上下文，避免全量历史带来的内存尖峰与请求体膨胀

### AI 用量同步优化
- **游标逐行同步**：从 opencode 数据库同步时不再把全量结果载入内存，边读边写，首次同步大批量历史时内存占用恒定

## 🐛 修复

### 供应商模型配置
- **供应商「消失」根治**：新增全局配置写锁，串行化所有读-改-写配置命令，修复 `save_chat_models` 与 `saveConfig` 用启动时旧快照整体覆写、导致已配置供应商被清空的问题
- **供应商分组修正**：多账号共用同一 base_url 但名称不同时各自成组，不再错误合并
- **供应商名称校验**：不能为空、不能重复，避免保存出无模型的空供应商

### 剪贴板
- **默认快捷键调整**：浮层呼出快捷键由 `Ctrl+Alt+V` 改为 `Ctrl+\``（反引号），进一步避开终端高频组合键
- **html 超限丢弃**：复制整页 / 大表格时 html 超过 64KB 只留纯文本，防止数据库膨胀
- **清理节流**：保留策略清理最小间隔 60s，避免高频复制时反复锁表拖慢监听与界面
- **进程名缓存上限**：防止常驻长期运行缓存无限增长，以及 PID 复用后返回陈旧进程名

### 性能 / 稳定性
- **浮层延迟回收**：剪贴板浮窗收起后延迟销毁 WebView2，短时间连续唤起零冷启动，长期不用才回收内存
- **窗口操作串行化**：粘贴 / 归还焦点统一交给单一 worker 线程执行，避免频繁创建短命线程
- **速记刷新轻量化**：外部保存速记后主窗口只拉取元信息刷新列表，不再全量读取正文

---

# v0.1.17 发布说明

## ✨ 新功能

### 剪贴板历史 v1.1：底层重构 + 交互增强
- **事件驱动监听**：用 `AddClipboardFormatListener` + `WM_CLIPBOARDUPDATE` 消息窗口取代 500ms 轮询，剪贴板一变即入库，实时且零轮询开销
- **富文本重试探测**：Office（Excel/Word）分阶段写入剪贴板时，按 `[0,40,80,140,220,360,560]ms` 递增延迟重试读取 HTML，避免丢格式；纯文本复制不触发重试
- **自复制回声抑制**：粘贴 / 复制历史项后，监听器不再把自己写入的内容再次入库（10s 指纹窗口）
- **粘贴更稳**：注入前释放全部修饰键（防 Ctrl+V 变 Ctrl+Shift+V）；`AttachThreadInput` + `BringWindowToTop` 恢复焦点，注入前校验目标窗口可见
- **浮窗可拖动**：顶部搜索栏按住可拖拽移动窗口（避开输入框与按钮）
- **单条删除按钮**：每条记录悬停操作区新增删除按钮，无需再右键

### AI 对话 Token 用量统计
- **会话级累计 token**：`chat_sessions` 表新增输入 / 输出 / 缓存 / 推理 token 与生成耗时字段，每轮回复完成后累加，用于面板实时统计
- **面板顶部实时统计**：输入框上方常驻展示「输入 / 输出 / 缓存 / TPS」四项指标
- **自动标题**：首轮回复完成后按内容自动生成会话标题（不再一直叫「新对话」）
- **流式 usage 解析**：请求开启 `include_usage` 并在流末尾解析 usage，兼容 DeepSeek 的 `prompt_cache_hit_tokens` 缓存别名

### 字体大小设置
- **全局字体大小**：设置 → 外观 新增全局字号滑杆（85%–130%），以根字号 rem 为基准一键整体缩放
- **单模块字体大小**：便签 / 速记 / 提示词 / 待办 / 用量 5 个内容模块可独立调整字号（相对全局叠加，默认 100%）
- **字号体系重构**：全站 200+ 处字号由硬编码 px 迁移到 rem / em 相对单位 + CSS 变量，实现「全局 × 模块」两层缩放

## 🐛 修复
- **Esc / 失焦关闭**：修复浮窗按 Esc 或点击外部失去焦点时无法关闭的问题（窗口权限缺失，`clipboard` 窗口未纳入 capabilities）
- **存为速记 / 加入提示词库**：修复点击后不生效——统一走 store 保存路径，保存后主窗口通过 `notes-changed` / `snippets-changed` 事件实时刷新列表
- **图标一致性**：存为速记改用 `FileText`、加入提示词库改用 `Boxes`，与主界面速记 / 提示词入口图标一致
- **去掉浮窗黑边**：Windows 透明窗口启用系统阴影会把边缘渲染成黑色描边，改为关闭 OS 阴影 + 显式透明背景色（面板自带 CSS 阴影不变）
- **顶部可拖动**：拖拽区域原排除输入框导致实际拖不动，改为整个搜索栏（含输入框空白处）位移超过阈值即拖，并抑制拖动开始瞬间的失焦自动关闭
- **在鼠标位置弹出**：每次唤起都重新定位到鼠标附近（原仅首次创建定位），并改用物理像素坐标修正 HiDPI 缩放下错位
- **暂停后恢复复制不进来**：恢复记录时清除自复制指纹，避免暂停期间产生的回声指纹抑制恢复后的首次复制
- **底部按键说明合并**：「单击 / Enter 粘贴」合并为一行
- **主窗口输入框粘贴无效（根治）**：浮层抢焦点导致 WebView2 输入框失焦，恢复焦点后注入 Ctrl+V 仍不可靠。改为唤起前是本应用主窗口时，由 Rust 直接把内容派发给主窗口，在 DOM 层把文本插回最后聚焦的输入框（便签 / 速记 / AI 对话等 input、textarea、contenteditable 均覆盖），不依赖窗口激活/焦点时序
- **粘贴出现重复条目**：相同内容不再新增条目——入库时按内容全量去重，重复即刷新时间挪到最前；粘贴 / 复制历史项后同步把该条目挪到列表最前
- **外部应用粘贴更稳（无激活显示）**：浮层改以 WS_EX_NOACTIVATE + SW_SHOWNA 无激活显示，不再一打开就抢走当前输入框焦点——唤起前聚焦的应用输入框全程保持焦点，纯鼠标点条目粘贴时无需任何焦点切换即可直接注入；点击搜索框开始键盘操作时才激活浮层（↑↓/Enter/Esc 生效），激活后点外部自动收起
- **终端 / 命令行粘贴支持**：新增「粘贴方式」设置（自动 / Ctrl+V / Ctrl+Shift+V / Shift+Insert）。自动模式下识别 cmd / PowerShell / Windows Terminal / WezTerm 等终端窗口，改用 Ctrl+Shift+V（部分终端不支持 Ctrl+V，仅支持 Ctrl+Shift+V 无格式粘贴）；键盘注入改为扫描码方式，兼容性更好
- **Esc 关闭归还焦点**：按 Esc 关闭浮层时把焦点还给唤起前的窗口，不再停在主窗口

## 🎨 界面优化
- **待办双击编辑改多行**：双击待办进入编辑时用自适应高度的多行文本域（回车换行、失焦保存、Esc 取消），不再挤成单行看不清内容；展示态保留换行
- **时钟卡片倒计时联动**：中上区块切到 Token / 速记 / 待办 / 速达时，时钟卡片的倒计时环形进度一并隐藏，切回「倒计时」恢复
- **Token 卡片精简**：去掉右侧「查看详情」文字，只留箭头图标按钮（悬停出 title）
- **粘贴方式下拉改用 AppSelect**：设置「粘贴方式」从原生 select 换成通用 AppSelect 组件，宽度自适应内容
- **更新说明默认开启**：升级后 What's New 弹窗默认开启（首次安装仍只记录不弹）
- **滚动条悬停显隐**：滚动条默认完全隐藏，鼠标移到可滚动区域 / 滚动条边缘时才淡入显示，移开即隐藏

---

# v0.1.16 发布说明

## ✨ 新功能

### 速记页改版：左右分栏
- **左侧目录 + 右侧内联编辑**：速记页不再弹窗编辑，左侧笔记目录（含标签筛选），右侧直接编辑（编辑 / Markdown 预览切换、标签管理、600ms 防抖自动保存）
- **未选中笔记空态**：右侧显示「选择或新建笔记」占位
- **底栏紧凑化**：标签行与保存时间同行（左标签右时间），面板上下间距收紧

### AI 对话交互优化
- **打开会话定位最新消息**：切换会话时自动滚动到最后一条（原停在第一句）
- **发送消息自动滚动**：发送后强制滚动到底部，流式输出期间智能跟随（上翻阅读不打断）
- **「跳到底部」悬浮按钮**：离开底部超过 40px 时在内容区右下角淡入出现，点击一键回到底部
- **头像对齐**：AI 消息头像顶部与首行文字顶部对齐，用户消息头像贴气泡顶部，流式输出同规则

## 🐛 修复
- 修复 `:deep(:first-child)` 被 Vue SFC 编译器编译为无 scoped 前缀的裸全局规则（污染全局样式、造成左右头像对齐反复失衡）——改为元素级确定性选择器
- 浏览器预览环境守卫补全：`addNote`/`saveNote`/`removeNote`/`createTag`/`deleteTag`/`persistTags` 增加 `isTauri()` 判断，`npm run dev` 预览不再报错

---

# v0.1.15 发布说明

## ✨ 新功能

### AI 对话系统
- **AI 对话右侧抽屉面板**：多会话管理（新建 / 切换 / 删除）、覆盖式悬浮布局（不挤压主区，可拖拽调宽 320–640 且记忆宽度）
- **OpenAI 兼容流式对话**：SSE 流式输出 + 打字机光标，Enter 发送 / Shift+Enter 换行，超时 300s，兼容 DeepSeek / OpenAI / Ollama / one-api 等端点
- **Markdown 渲染**：AI 回复支持标题、列表、引用、链接、表格、代码块、行内代码等完整 Markdown 排版，流式过程实时渲染
- **API Key 安全存储**：密钥存入系统钥匙串，界面上脱敏展示（保留首尾 4 位），支持小眼睛查看 / 一键复制
- **面板透明度调节**：设置 → AI 助手 新增滑杆（50% – 100%），整体不透明度实时预览并持久化；拉满 100% 为近实心背景
- **快捷键**：`Ctrl+Shift+K` 全局唤起 / 收起对话面板；标题栏对话按钮入口

### 供应商模型配置
- 设置新增「AI 助手」分类，以**供应商**为管理单位（卡片式列表，默认折叠）
- 新增供应商后可**测试连通性**（验证 URL + API Key）、**拉取模型列表**并勾选批量添加
- 同一供应商下模型自动共享 API Key（按 base_url 传播）
- 对话区模型下拉**按供应商分组**展示，选中后仅显示模型名，下拉加宽至 260px+
- 已配置模型以 **tag 并排展示**（自动换行），可复制名称 / 删除；默认模型高亮标注

### 界面优化
- **全局滚动条美化**：悬浮留白 + 品牌色悬停 + 暗色适配
- **欢迎页移除版本号**，标题栏去图标改 X-Hub
- **时钟语录可配置**：设置通用区可开关（默认「日拱一卒」）
- **省略号悬停显示全文**：全项目排查所有 `text-overflow: ellipsis` 位置补齐 title 提示（16 个文件）
- 用量趋势柱高、明细分页自适应、CountdownFloat 水位渐变跟随强调色等渲染修复

## 🐛 修复
- 全局快捷键 / 托盘 toggle 状态机：最小化或被盖住时一次按键直达窗口，仅可见且聚焦时才隐藏
- 主题令牌隐患：`--brand-400` 未定义回退、硬编码 `#fff` 改 `--text-on-accent`、TitleBar 图标继承 currentColor
- UsageView 明细 key 唯一化
- AI 对话面板透明度拉满 100% 时背景仍透光的问题（新增近实心 `--bg-chat-panel` token）

## 📌 说明
- 侧边栏「对话」入口暂时隐藏（后续恢复），AI 对话仍可通过标题栏按钮或 `Ctrl+Shift+K` 唤起
- 成本和花费相关功能暂不包含（按规划去除）
